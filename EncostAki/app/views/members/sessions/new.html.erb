<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
    <title>Encostaki</title>
    <link href="https://fonts.googleapis.com/css?family=Bree+Serif" rel="stylesheet">
    <link rel="icon" href="imgs\logopinfinal.png" type="image/x-icon" />
    <link href="http://maxcdn.bootstrapcdn.com/font-awesome/4.1.0/css/font-awesome.min.css" rel="stylesheet">
	  <%= stylesheet_link_tag "css/bootstrap" %>
    <link href="css/login.css" type="text/css"  rel="stylesheet">
</head>

<body>
	<div class="container-fluid">
		<div class="row">
			<div class="col-md-4 col-sm-4">
				<div id="esquerda-l">
				</div>
			</div>
			<div class="col-md-4 text-center col-sm-4 text-center">
        <div id="centro">
          <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
            <h1>Login</h1>
            <h2><%= image_tag "logopinfinal",id:"logo"%></h2>
            <div class="field">
              <p><%= f.label "Email" %><br/></p>
              <%= f.email_field :email, autofocus: true %>
            </div>
            <div class="field">
              <p><%= f.label "Senha" %><br/></p>
              <%= f.password_field :password, autocomplete: "off" %>
            </div>
        
            <% if devise_mapping.rememberable? -%>
              <div class="field">
                <p><%= f.label "Lembrar senha" %></p>
                <%= f.check_box :remember_me %>
              </div>
            	<p>Não possui conta? <%= link_to "Cadastre-se", new_member_registration_path %></p>
        <% end -%>
        <div class="actions">
          <%= f.submit "Log in"%>
        </div>
      <% end %>
      
      <%= render "members/shared/links" %>
