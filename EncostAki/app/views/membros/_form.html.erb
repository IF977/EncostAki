<%= form_with(model: membro, local: true) do |form| %>
  <% if membro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(membro.errors.count, "error") %> prohibited this membro from being saved:</h2>

      <ul>
      <% membro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :admin %>
    <%= form.check_box :admin, id: :membro_admin %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :membro_email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
